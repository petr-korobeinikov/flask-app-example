<html>
<head>
    <title>flask-app-example</title>
    <link rel=stylesheet
          type=text/css
          href="{{ url_for('static', filename='bower_components/bootstrap/dist/css/bootstrap.min.css') }}">
</head>
<body>
<div class="containter-fluid">
    <nav class="navbar navbar-light">
        <a class="navbar-brand" href="{{ url_for('index') }}">flask-app-example</a>
        <ul class="nav navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ url_for('post_index') }}">posts</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url_for('post_new') }}">new post</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url_for('person_index') }}">people</a>
            </li>
        </ul>
        {% if current_user.is_authenticated %}
            <ul class="nav navbar-nav pull-xs-right">
                <li class="nav-item">
                    <a class="nav-link" href="#">{{ current_user.username }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('signout') }}">sign out</a>
                </li>
            </ul>
        {% else %}
            <ul class="nav navbar-nav pull-xs-right">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('signup') }}">sign up</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('signin') }}">sign in</a>
                </li>
            </ul>
        {% endif %}
    </nav>
</div>
<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-3">flask-example-app</h1>
        <p class="lead">This is a simple application written in Python using Flask.</p>
    </div>
</div>
<div class="container-fluid">
    {% for message in get_flashed_messages() %}
        <div class="flash">{{ message }}</div>
    {% endfor %}
    {% block body %}{% endblock %}
</div>
</body>
</html>
