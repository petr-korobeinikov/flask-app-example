{% macro form_row_submit(text) -%}
    <div class="form-group row">
        <div class="col-sm-10 col-sm-offset-2">
            <input type="submit" value="{{ text }}" class="btn btn-primary">
        </div>
    </div>
{%- endmacro %}
